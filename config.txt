# Default maze configuration file
# Lines starting with # are comments

# Maze dimensions
WIDTH=15
HEIGHT=16

# Entry and exit coordinates (x,y format)
ENTRY=0,1
EXIT=6,5

# Output file for the generated maze
OUTPUT_FILE=output_maze.txt

# Perfect maze flag (True = single path, False = multiple paths)
PERFECT=False

# Optional: Random seed for reproducibility or remove if not needed
SEED=123

# Maze generation algorithm (dfs, kruskal, prim)
ALGORITHM=prim

# Pathfinding algorithm (bfs, dfs, astar)
PATHFINDING_ALGORITHM=astar
